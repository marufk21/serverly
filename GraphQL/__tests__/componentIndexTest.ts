// Test file to verify that components can be imported from the ui index file
import { Button, Card, Input, Label, Textarea } from "@/components/ui";

// Simple test to verify imports work
export function testComponentIndexImports() {
  console.log("Component index imports test passed");
  return true;
}
